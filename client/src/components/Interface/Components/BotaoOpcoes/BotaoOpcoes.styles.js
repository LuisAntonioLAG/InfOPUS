import { makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({

    fullWidth: {
        whiteSpace: 'nowrap',
        width: '100%',
    },
    dropOptions: {
        width: '10%'
        },
    }));