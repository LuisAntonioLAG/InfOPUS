import { makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({


    root: {
        flexGrow: 1
      },
    titleIcon: {
        marginRight: 15,
    },
    cardIcon: {
       fontSize: '5em',
        margin: 'auto',
    },
    fullWidth: {
        width:'100%',
    },
    button: {
      },
}));